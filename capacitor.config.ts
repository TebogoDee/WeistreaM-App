iimport { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.za.weistream.WeistreaM',
  appName: 'WeistreaM',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
